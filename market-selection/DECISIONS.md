# DECISIONS.md — market-selection session, 2026-08-02

Ambiguities resolved without asking, per the operating mode. Conservative
reading taken every time. Newest last.

| # | Ambiguity | Conservative reading taken | Why |
|---|---|---|---|
| D1 | Where to put new work — repo root or a subdir? | `trading/market-selection/`, new subdir, code tracked and `data/` gitignored | Matches the existing five-project layout. Nothing outside this directory was modified except `HANDOFF.md`. |
| D2 | STATUS.md says PIDs 17892 and 24756 must not be touched | Left both alone; verified alive (17892 up since 08-01 02:58, 24756 since 08-01 13:42) and did not read, move or restart them | They accrue the only non-re-pullable asset. Confirmed running, not assumed. |
| D3 | "Paced, single-threaded API calls" vs. three crawls at once | One request in flight **per host**. Kalshi, gamma-api.polymarket.com and r2kalshi.pmxt.dev are different hosts, so they run concurrently; nothing runs two streams at one host | The instruction is API courtesy. Serialising across unrelated hosts would waste the night for no politeness gain. The mirror uses 3 workers against an R2 CDN bucket, which is a static-file host, not an API. |
| D4 | Trade-tape window: how far back? | **24 h**, cut from an initial 48 h | The tape is far denser than expected — 100,000 trades covered only 25 minutes of market time, so 48 h ≈ 11.5 M trades, ~2 h of pulling and ~5 GB, competing with the mirror for bandwidth. 24 h is a full daily cycle, so it carries no time-of-day bias, at half the cost. |
| D5 | pmxt described as "rolling 30-day retention, deletes itself continuously" — mirror first or verify first? | **Verified first** (≈15 min of HEAD probes), then mirrored | Verification disproved the premise: the archive is frozen, not rolling. Had it been true, the probes would have cost 15 min of a 30-day window. Had I mirrored blind, I would have reported a false urgency as fact. |
| D6 | Which price fields to read from Kalshi | `*_dollars` / `*_fp` only, never the legacy integer-cent names | Measured: of 200 open markets, `yes_bid`, `yes_ask`, `no_bid`, `no_ask`, `last_price`, `volume`, `open_interest`, `liquidity` were non-null on **0**. GUARDS #12 predicted this; it is now confirmed live. |
| D7 | Polymarket family key — market-level or event-level crawl? | Event-level (`/events`) | Tags are the only family-like key Polymarket exposes, and they are returned on `/events` but not on the event stub inside `/markets`. A market-first crawl has no family key at all. |
| D8 | Kill-switch threshold — what counts as "a counterparty exists"? | Pre-registered **before** looking at the per-series tape: a family passes only if it clears **all** of (i) ≥100 trades/day, (ii) ≥20 distinct markets traded/day, (iii) ≥50% two-sided quote uptime. Anything below any one of them is killed. | Fixing the gate before seeing the numbers is GUARDS #10. Recorded here at the time the tape was still downloading, so it could not have been fitted to the result. |
| D9 | The 419,828-market count looked implausibly large | Checked for duplicate tickers before using it: **419,828 rows, 419,828 distinct tickers, 0 duplicates** | The pagination could have cycled. It did not. But 83% of the universe is two exotic parlay series, which is the real explanation and matters for Task 1. |
| D10 | Polymarket depth: dedupe markets across tags, or let them count once per tag? | **Count once per tag.** Removed the global token dedup and cached the HTTP response instead | Tags are overlapping views, not a partition. The deduped version gave whichever tag was probed first the market and left later tags at n=1–2, which is not a measurement. |
## Soccer session (STEP 3)

| # | Ambiguity | Conservative reading taken | Why |
|---|---|---|---|
| S1 | Team-name join: hand-write an alias table, or resolve programmatically? | **Resolve against each league's closed ESPN roster** by token overlap with an unmatched-token tie-break; aliases only where no token is shared | Each league has 20–30 clubs, a closed set. A hand alias table is long, unverifiable and silently rots. The first version matched 48.8%; this matches 98.8% and is covered by a 34-case test suite. |
| S2 | ESPN publishes both a displayed minute and a `wallclock`. Which to join on? | **`wallclock`**, always | It is an absolute UTC instant. Measured, a minute-based join is off by a median 17.5 min. Using the minute would have introduced an error where none needed to exist. |
| S3 | Pinnacle is absent from 2026. Abandon the benchmark or substitute? | **Substitute `AvgC*` (market-average close) and label it clearly as a consensus line, not a sharp one** | A weaker benchmark honestly labelled beats no benchmark, but the distinction has to survive into every downstream claim. |
| S4 | The in-play study conditions on "the team scored". Report the pre-goal price drift? | **Report it, and name it as a selection effect, not a signal** | The +4¢ T−5→T−1 drift is a property of conditioning on the outcome. Left unlabelled it reads as anticipation. |
| S5 | Feature leak prevention: stamp-and-check, or structural? | **Structural.** Matches are processed in date order and enter the history only *after* their own features are computed | A future match cannot influence a past one because the information is not in the accumulator yet. Stronger than a stamp that someone can get wrong — as I did, 159 times, on the first run. |
| S6 | `last_match` (drives rest days) — update on every fixture or only completed ones? | **Only completed** | Otherwise a postponed or abandoned fixture resets a team's rest clock without a match having been played. |
| S7 | ESPN backfill window size | **7 days with a 1-day overlap** | A 10-day range returned 85 MLS matches where ~170 existed — ESPN caps a response regardless of `limit`. The overlap then caused an infinite loop at the terminal window, which needed an explicit break. |

| D14 | The shortlist's #1 mechanism was "no free public reference exists for MLB props". Assert it, or test it? | **Tested it, and it failed.** ESPN's free odds object carries a `propBets` reference resolving to 555–677 priced DraftKings props per game | The tasking is explicit that a shortlist entry needs a mechanism. A mechanism I have not tried to falsify is a hope. Testing it cost 20 minutes and removed an entry's whole basis. |
| D15 | Kalshi-vs-DK props read "median gap 4.55¢, 79% exceed the cost bar" — publish it? | **No. Reported INCONCLUSIVE.** DK prop entries are one-sided, so the implied probability carries the book's margin; the −3.52¢ mean signed gap is what a 4–7% vig looks like. A second pass found **zero** de-viggable two-sided prop lines | Publishing it would have been publishing the vig as an edge. This is the C006 failure mode — benchmarking against a strawman — and the archive already has one of those. |
| D16 | Report the measurement anyway as "suggestive"? | **No.** An uninterpretable number is not weak evidence, it is no evidence | GUARDS #1's three-valued verdict: UNTESTABLE is never rendered as a pass. The same logic applies to a confounded comparison. |
| D11 | Discovered mid-session that the Kalshi trade tape retains exactly 69 days and rolls daily, so the trades matching the pmxt order-book window are expiring one day per day. Chase it, or stay in scope? | **Started a backfill immediately**, oldest day first (2026-05-25 →), in the background | The session's operating mode is explicit that anything accruing in wall-clock time takes priority over analysis, and this is the same asymmetry pointed the other way: an hour of delay costs a day of trades permanently. It also directly completes the pmxt mirror, which is depth-only and cannot answer "what would have filled" without the tape. 2026-05-14→05-24 is **already gone**. |
