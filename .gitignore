# ---------------------------------------------------------------
# This repo is PUBLIC. Code, docs and small result artifacts only.
# Every recorded dataset stays local. Nothing credential-shaped
# is ever tracked.
# ---------------------------------------------------------------

# --- recorded data directories (all projects) ---
data/
Data/
raw/
candles/
candles_*/
depth/
panel/
panels/
spot/
deribit/
binance_1s/
poly_books/
poly_trades/
kalshi_quotes/
kalshi_settled/
btc15m_opens/
synth_null/
synth_boost/
mm/

# --- recorded data formats, anywhere ---
*.parquet
*.parquet.*
*.jsonl
*.ndjson
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*-wal
*-shm
*.bak
*.bak-*
*.db.bak*
*.npz
*.h5
*.hdf5
*.feather
*.arrow
*.pkl
*.pickle

# --- secrets: never track any of these ---
.env
.env.*
*.env
!.env.example
*.pem
*.key
*.p8
*.p12
*.pfx
*.jks
*.keystore
*.crt
*.cer
*.der
id_rsa*
id_ed25519*
id_ecdsa*
*_rsa
*_dsa
.netrc
_netrc
.npmrc
.pypirc
credentials
credentials.*
*credentials*.json
*secret*.json
*secrets*.yaml
*secrets*.yml
.aws/
.ssh/
kalshi_key*
kalshi-key*
*private_key*
*privatekey*

# --- node / frontend (polymarket-tennis-copy ships a React dashboard) ---
node_modules/
.next/
.turbo/
.parcel-cache/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# --- python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
ENV/
.Python
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.coverage
htmlcov/

# --- os / editor ---
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# --- local agent state (may contain machine-specific paths) ---
**/.claude/settings.local.json

# --- exported chat transcripts: personal data, not project work ---
kalshi-chat-audit/max_convos/
kalshi-chat-audit/pro_convos/

# --- desktop machine (added 2026-08-03, see DESKTOP_INVENTORY.md) ---
# Discord server export: names real people and their trading calls. The corpus
# is kept on disk because it is the only copy and the largest untested item
# available, but it is personal data and this repo is PUBLIC.
discord-trades-export/
# Live-account state: bot_state.json carries real Kalshi order ids.
bot_state.json
# Recorder mutex - must never travel between machines.
*.lock
.recorder.lock

# --- byte-identical local backup, kept on disk, not in the repo ---
_archive/

# --- large raw API dumps (regenerable, not results) ---
kalshi-tennis/audit/series_raw.json
kalshi-tennis/data/kalshi/tennis_markets.json
crypto/docs/*_raw.txt
crypto/docs/strikes_raw.txt

# --- logs ---
*.log
*.err

# --- signal-github: recorded API responses and judgments about named repos ---
signal-github/cache/
signal-github/reports/
signal-github/GITHUB_KNOWLEDGE.md
signal-github/PLAIN_ENGLISH.md

# --- extractor-upgrade: per-document judgments about named creators/authors ---
extractor-upgrade/reports/
extractor-upgrade/frames/
