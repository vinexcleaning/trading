# DECISIONS.md — judgement calls made without asking

Overnight mode, 2026-08-01. Conservative reading taken in every case.

| # | Ambiguity | Decision | Why conservative |
|---|---|---|---|
| 1 | Maker fee **rate** on ATP/WTA. `fee_type: quadratic_with_maker_fees` is verified from the API; the rate is not exposed read-only. | Used Kalshi's published 0.25¢/contract flat, **and** reported a pessimistic arm at ¼-of-taker everywhere. | Both arms are reported side by side, so no result depends on the unverified number. It affects ~9% of the book; Challenger/ITF is verified `quadratic` = **zero** maker fee. |
| 2 | G2 (pre-match residual ≤1.5 pp) failed on a partial single-tier smoke sample while G1/G2b passed. | Added **G2b** (orientation *difference*) as the leak-specific blocking test; left G2's threshold untouched; made a G2-only failure exit 3 = "proceed with caveat", not exit 0. | The pre-registered threshold was not relaxed after seeing a number. On the full clean sample G2 passed anyway (+0.21/+0.55 pp), so this never bound. |
| 3 | Which side to record depth for, given API budget shared with two other sessions. | One market per event, chosen by **lexicographically first ticker** — the same outcome-independent rule as the universe. | Halves request load and cannot introduce the selection leak that voided Phase 2. Mirrors agree to 0.00¢ median, so one side is sufficient. |
| 4 | Whether to run Phase 3 segmentation after the clean re-run. | **Not run.** θ = −2.42 pp against a 3.70 pp cost bar; the fade loses in all 6 configurations and every maker cell is negative. | The pre-registration says do not slice a non-clearing effect into subgroups. That is how this project produced its false positives. |
| 5 | Hypothesis ledger continuity across the retraction. | **Reset** for the clean run (54 entries). The 90 void Phase 2–4 entries are reported separately as *void*, not carried as tested hypotheses. | Carrying void tests into the FDR denominator would understate significance; dropping them silently would hide the history. Both counts are stated. |
| 6 | My pre-registered prediction (θ ≈ +1.31 pp, interval [−1, +3]) failed. | Recorded as a **failed prediction**; interval **not** revised; the reason for the failure diagnosed and written up. | The pre-registration exists precisely to stop post-hoc rationalisation. |
| 7 | 164 markets failed the candle refetch (0.83%). | Retried once single-threaded; **all 164 recovered**; merged. Zero unrecovered. | No silent sample loss. |
| 8 | Depth recorder cadence with two sibling sessions on the same API. | Single-threaded, 0.55 s between requests, ~1 snapshot/market/minute, exponential backoff on 429, market list refreshed every 20 min. | Matches candle resolution without adding concurrency. Throughput has already been halved once by concurrent pulls. |
| 9 | `plausible` duration filter is UNTESTABLE (dropped arm 718 rows, MDE 2.75 pp) and reads z = −3.53. | Kept the filter, flagged as untestable, and reported the z. Did **not** treat "untestable" as "fine". | Removing it would admit implausible 1,000-minute windows; keeping it silently would hide a 3.5-sigma signal. Stated as a known open weakness. |
| 10 | The four-way P&L decomposition maps imperfectly onto a passive directional entry (there is no two-sided spread capture). | Reported in the crypto session's units for comparability, with the mapping stated explicitly and the differences named. | Comparability without pretending the two experiments are the same object. |
| 11 | `plausible` filter unresolvable by residual test (#9 above). | **Resolved by a different instrument**: compared θ with the filter on vs off. +0.02 pp [−2.05, +2.16] — immaterial. Filter kept. | The residual test asks "are the dropped rows priced differently"; the decision-relevant question is "does the filter move the answer". The second is answerable at full power. |
| 13 | **Task 1 (Flashscore labelling) is impossible.** Two independent blockers, both verified today. (a) The actor's `dayOffsets` enum is **−7 to +7**; my window needs −68 to 0. (b) Apify returns **"Monthly usage hard limit exceeded"** — no actor runs at any price, so the approved $3.44 cannot be spent. | Logged as blocked; did not attempt workarounds or alternative paid actors (all cost more and the account is capped anyway). Reprioritised to Task 2 on existing labels. | The operating rules say a disproven premise is a finding: stop the thread, report it, reprioritise. Burning further calls to confirm an explicit account-level cap would waste quota that other sessions may need. |
| 14 | Task 2 (C3) was specified as "reuses Task 1's data", which now does not exist. | Ran it on the **existing 2,887-row truth set**, which already carries `s1_w`/`s1_l` game scores — set-1 margin and tiebreak are directly derivable without Flashscore. | The blocker removes the *extra* labels, not the *existing* ones. Task 2's buckets are computable today at n=479; reporting them with honest MDEs beats skipping the task. |
| 12 | The residual guard flags *every* duration-derived filter (z = −3.5 to −6.2 at all five bands tried). | Recorded as a **known limitation of the guard**, not as five separate leaks. Match duration is genuinely outcome-correlated in tennis — the sub-25-minute bucket wins 93.4% because those are retirements. | Treating a real sport property as a leak would delete a filter that removes non-matches. The θ-invariance test is the correct instrument for duration filters. |
| 15 | **2026-08-20.** Mailbox `017` gated the maker test on a data question. I answered "cannot be run" from two local archives (`bot-hunt/record.db`, `kalshi-market-scan`'s one-day tape). | **Reversed.** The Critic flagged the phrase as an absence claim; probing the live API showed trades *and* one-minute bid/ask candles are retrievable for settled markets back to 2026-06-14 — **35,994 markets, 17,997 matches.** The wrong answer is kept at the top of `MAKER_DATA_AUDIT.md` rather than deleted. | Eighth instance of the repo's documented failure shape: read one source, concluded, stated it confidently. Deleting the wrong answer is how someone re-derives it. |
| 16 | Whether to pull trades for all 35,994 markets. | **No — two passes.** Candles for everything (~1 GB), trades only for markets where the entry rule fires. | Measured on a smoke run: 616 candle rows vs **4,011 trade rows** per market, i.e. ~28 GB of trades, nearly all in markets that never trigger and therefore cannot affect any result. |
| 17 | Pull now, or finish the write-up first. | **Pull first.** Started before the audit was finished. | The retention floor advances one day per day and a closed market 404s forever. Pulling is free, read-only and reversible; the lost day is not. Three weeks of the study's window are already gone. |
| 18 | How to read `taker_book_side`, which is perfectly redundant with two other fields. | **Resolved empirically, not from the field name**: compared each trade's price with the prevailing quote. `bid` prints at the **ask** 4,485 times vs 1,589 at the bid — it is the *taker's own* order side. | The natural reading gives the direction backwards, and the direction is what decides whether the fade's resting bid is the easy or the hard side to fill. It is the hard side. Flagged repo-wide in `STATUS.md`. |
| 19 | The pre-registration initially fixed depth at `deep:30@38`, which would have silently dropped `deep:40` — the user's actual suggestion. | **Changed before any result existed.** The whole depth grid including `deep:40` is *looked at* and reported as a chart; only the three registered arms get a verdict. | §9c step 7: slicing is fine for looking, not for concluding. This repo has already killed a live idea by letting a sweep close a question it never tested. |
| 20 | Which period to hold back as the untouched check. | **A date split: 2026-08-02 → 08-20, 4,132 matches.** Not a random split. | The original study's selection window *ended* 2026-08-01, so those days were never fitted by anyone. A random split would have leaked the original selection into the check set. |
