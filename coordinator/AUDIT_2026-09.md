# THE ASSUMPTION AUDIT — running record

**Started 2026-09-02 at his request, after his diagnosis: "the bot is assuming
too much."** Read-only against everything except this file and inline
corrections filed by house rules. Each finding names what was checked, what was
found, and where the fix was routed. **A clean check is recorded as loudly as a
catch, so the next reader knows what was already looked at.**

---

## CATCH 1 — my own calibration table used 22,974 rows the source study had rejected

`coordinator/studies/calib.py` filtered on `pre_bid IS NOT NULL` and never read
the `ok` flag that `set1_overshoot`'s `p6_state.py` stores on every row. 67% of
the sample was `pre-match book empty` — quote present, spread wider than
`PRE_SPREAD_MAX = 10`, mid meaningless. **Corrected inline in
`RESEARCH_PROGRAM.md` (commit `6b44936`), wrong table left visible.** Clean-row
story: gap smaller and patchy, two mid bands negative, one band (80–85c) at
**+0.8%** at the ask — not a finding. Practical conclusion unchanged. Tennis's
maker test unaffected (its bands were 96%+ clean rows).

**The rule this yields: an analysis that reads another study's table MUST honour
that study's own `ok`/`why` flags, and say so.**

## CLEAN — provenance of stored data against Kalshi's live API (2026-09-02)

| what | sample | result |
|---|---|---|
| tennis results, `maker.db` | 6 newest settled | **6/6 match** |
| baseball results, `kalshi_truth.db` | 8 newest settled | **8/8 match** |
| baseball minute candles, bid & ask | 10 candles | **10/10 match to the cent** |
| baseball earliest-price timing | all 1,706 | **all strictly before first pitch** |

**Limit stated:** spot checks on recent markets. Kalshi's ~69-day window means
older rows can never be externally verified again — the recorded copy is the
only copy, which is why the recorders exist. A standing re-run of this check
belongs in the recorder health pass, on markets young enough to verify.

## CORRECTION TO MY OWN CLAIM — "89 rules, a handful enforced" was wrong

`GUARDS.md` has **27 numbered guards** (my 89 counted every heading). Scorecard:

- **15 TESTED** — a test file references and asserts them (#1–7, 9, 13, 14, 18,
  20, 21, 22, 23)
- **9 referenced in code but not asserted by any test** (#8, 10, 11, 12, 15, 19,
  24, 25, 27)
- **3 prose only** (#16 dedup-headline, #17 argmax null, #26 rows-vs-people)

The enforcement picture is materially better than I reported. The gap that
remains: #10 (pre-registration before numbers) and #17 (argmax null) are the two
most load-bearing unenforced ones — both guard against exactly the best-of-N
failures this repo keeps having.

## CLEAN — the fee guard is exemplary, and my 12 "suspects" were already handled

`test_no_fee_reimplementation.py` scans the repo for fee-shaped arithmetic and
maintains a reasoned allowlist. All 12 files my sweep flagged are either
allowlisted with a written reason (quoted prose, refutation subjects, audits of
other people's claims) or non-matching. **Test passes.**

## CATCH 2 — two settled claims about Polymarket's fee disagree, and the sampler uses the weaker one

`C004` (SETTLED, 4,310 on-chain fills, April, reproduced on 5,362): the real
fee was `0.10 × min(p,1−p)` and the documented `0.07·p(1−p)` matched **0.0% of
fills**. `BH025` (three days old, docs page only, zero fills): sports are
`0.05·p(1−p)` taker. **Both can be right — different era, maybe different
products — but this repo has already proven once that Polymarket's docs did not
match what it charged.** Routed to `devig` (mailbox 026): verify against real
on-chain sports fills before the sampler's "executable after fees" column is
believed; until then the fee is labelled docs-only.

---

## STILL TO DO

1. **Assumed-numbers sweep beyond fees** — spreads, slippage constants,
   probabilities, sample-size claims hardcoded in active `src/` folders.
2. **The two unenforced load-bearing guards** (#10, #17) — scope what a test
   for each would look like; do not build without a decision.
3. **Recorder-side provenance** — the same spot-check discipline for
   `record.db`'s Polymarket and Pinnacle legs, which tonight's pass did not
   touch.
4. **Folder-by-folder pass** over the 13 test-less folders, dormant ones last.

---

## PASS 3 — overnight run, 2026-09-01

## FALSE ALARM CAUGHT IN MYSELF — the "recorder dead for two days" scare was my own clock

The newest tape rows read `2026-09-01T04:48Z` and I read that against sync
dates I had been writing as September 2nd. **Local time was 00:48 on September
1st — the recorder had written 14 minutes earlier.** Every runner is alive:
both cross-venue recorders, the factory's wide recorders, both paper tests,
**and the paired sampler and props capture are already built and registered in
`runners/runners.json`** — devig had finished and wired them without being
chased. My recent sync-marker dates had drifted a day ahead; same class of
error as the 6.5-minute clock skew that faked 1,292 arbitrages. Corrected.

## ASSUMED CONSTANT — `SLIPPAGE_C = 1.0` gates every baseball bot entry, never measured

`mlb-paper/src/mentalities.py:79`. A flat assumed cent subtracted from every
edge before a bot may enter, in the live decision path and the replay. It
shapes WHICH bets fire. **Measurable now from `livedesk`'s real fills** (asked
36c, filled 33c — the desk records both sides). Routed to `mlb` as a note.

## PROVENANCE, other two recorder legs — instrument works, hour was wrong

Polymarket: the public book endpoint 403s a bare client and answers one with a
browser user-agent; at 1am every book was settled/empty (1/99 placeholders), so
the value comparison was **inconclusive, not failed** — `test_poly_sides.py`
already guards orientation. Pinnacle: guest API live, 1,149 markets, zero
overlap with recorded rows because those games settled. **Both re-runs queued
for game hours.**

## EVIDENCE-FILE SCAN — 651 references behind all seven ledgers

**627 resolve on disk. 1 is data not on this machine. 23 are truly missing —
and they cluster:** ~19 are pre-migration scripts behind still-cited SETTLED
claims, including `src/p5_segment.py` (S005, "0 of 25 buckets clear"),
`src/phase4b_shrinkage.py` (W015, the shrinkage result), `src/exit_study.py`
and `src/hold_settle.py` (C022/C023 era), plus chat-export JSONs that
`LAPTOP_COPY.md` already documents as laptop-only. **The claims are not
suspect — their generating scripts live on the laptop and never made the
machine switch.** Fix is a two-minute copy next time the laptop is open;
exact list kept here.

## SCOPED, NOT BUILT — tests for the two load-bearing prose guards

**#10 (pre-registration before numbers):** mechanically testable — for every
`reports/`-style results file, assert a `PREREGISTRATION_*.md` exists in the
folder whose FIRST git commit predates the results file's first commit.
Grandfather existing files by date. ~60 lines plus the grandfather list.
**#17 (the argmax null):** not mechanically testable without false positives;
the honest home is a required line in `coordinator/REFLECT.md`'s Critic list —
"where a best-of-N appears, the N and a shuffle control must appear beside
it." Both are decisions for him, not built.

## PASS 4 CLOSED — what was filed, and what is his

**Filed to owners:** `devig` 027 (bot-hunt + market-selection) · `tennis` 022
(the live-money client, the naked-short path) · `signal` 014 (the two moving
numbers, the guard that cannot fail) · `livedesk` 024 (payload-shape gap) ·
`mlb` 025 (laptop interpreter).

**Method note for the next reader.** Eight folders were read by five parallel
readers plus me. **I re-verified every CERTAIN finding against the code before
filing, and corrected three of them:** the "no canary test" claim about
`kalshi_client.py` (livedesk does test the switch, in both directions) · the
unclamped-price bug (real, but 0 of 2,205 scorecard rows hit it) · the
fee-at-mid error (real, but measured at ≤0.2c and conservative in direction).
**Readers find; the filer verifies.** A finding that reached an owner
unverified would be the same failure this audit exists to catch.

**HIS, and only these three:**
1. **The Pinnacle guest key in `probe_pinnacle.py:40`** — public, not his,
   leaks nothing, but it is credential-shaped in a PUBLIC repo. Raised once.
2. **The 97.4% line in `CLAUDE.md` §9b** — the dictator does not own that
   file. Corrected wording comes from `signal`; he approves the edit.
3. **Sofascore polling at 10s** against the feed module's own 60s floor —
   a ban would take the live score feed with it. Judgment, not arithmetic.

## STILL OPEN FROM PASS 2
Recorder-leg value checks in game hours (mine) · assumed-numbers sweep beyond
fees and slippage (partial) · folder-by-folder over the 13 test-less folders
(the cited-claims part is covered by the evidence scan; code-reading pass
remains).

## PASS 3 (cont.) — tennis prices verified, and the live-money constant skim

**Tennis candles 8/8 match the API to the cent** — all three databases now
verified on results AND prices where the venue still serves them.

**`livedesk` constants (mailbox 023 to livedesk):** the on-screen "room for N
more bets" divides by the dead flat $4.15 stake and understates his room ~2x ·
`BANKROLL_START = 83.00` is a silent default for his starting balance ·
`MAX_ORDERS_PER_DAY = 9999` is a no-op living under a banner calling it his
number · the $1.00 reconcile tolerance swallows sub-dollar drift unlogged.
None urgent while the desk is parked; all filed before it runs again.

**Critic checklist:** the argmax line (#17) added to `REFLECT.md` §3 — my own
file, judgment call logged in `coordinator/DECISIONS.md`. The repo-wide test
for #10 remains scoped-only, his decision.

---

## PASS 4 — the code read over the test-less folders, 2026-09-01 (daytime)

His instruction: finish this on Fable, then the dictator goes back to Opus.
Method: I read `mlb`, `reopen` and `QWEN_BATCH_2` line-level myself; five
parallel readers (same model) covered `bot-hunt`, `market-selection`,
`kalshi-tennis` + `kalshi-inplay-bot`, the signal family, and `bot-forensics`;
their CERTAIN findings get verified by me before anything is filed.

### FOUND — `QWEN_BATCH_2/` is a stale fork of live-money-adjacent code, unknown provenance

Seven files at the repo root, untracked, dated Aug 3–12: an old copy of
`mlb-paper`'s brain (`engine.py`, `mentalities.py`, `run.py`), old copies of
`livedesk`'s demo-execution tests, the `kalshi-inplay-bot` client, and an
IDENTICAL copy of `livedesk/PRACTICE_SETUP.md`. Every code file differs from
today's live copy (all are smaller = older). **No secrets inside (checked).**
The name suggests a batch prepared for the Qwen model. Risk: someone runs the
stale brain and gets numbers contradicting `mlb-paper`, or it gets committed
to the PUBLIC repo as apparent-current code. **Not deleted — I didn't create
it; his call.** Recommendation: delete or move under `_archive/`.

**RESOLVED 2026-09-01, his decision: it stays.** It is his Qwen workspace —
he codes through Qwen when Claude usage runs out. Made safe instead of
deleted: `QWEN_BATCH_2/` added to `.gitignore` (commit `862c771`) so it can
never publish, and `READ_ME_FIRST.md` placed inside listing which live file
each copy is behind, with the one-way rule (refresh live → workspace, never
back).

### FOUND — `mlb/src/run_pipeline.py:16` hardcodes the LAPTOP interpreter

`PY = r"C:\Users\gianf\..."` — the overnight pipeline chain crashes on this
machine at stage 1. One-line fix (`sys.executable`); routed to `mlb`'s
mailbox, their folder. Minor in the same folder: `inplay_rfi_latency.py:168`
comment says the move threshold is "or >= 90c", code says `80.0` — cosmetic,
conclusion (market moves before the feed → dead) robust to it.

### `market-selection` — read in full (46 files). Every CERTAIN finding re-verified by me.

**No conclusion in `LEDGER_ADDITIONS.md` is overturned.** Two real defects,
both operational, plus one latent bug that turned out to have touched nothing.

**FOUND — the folder cannot be re-run on this machine.** `market-selection/data/`
does not exist on the desktop (verified). Every analysis script reads it; the
57 frozen JSONs in `reports/` are all that remains here. Its claims are
take-on-trust artifacts until the laptop is reachable. Two scripts also
hardcode the laptop user: `mirror_pmxt.py:34` and `resolve_orderbook.py:24`
(`C:\Users\gianf\...`). `mirror_pmxt.py` would `makedirs` that path and start
re-downloading a 662-file archive into a fresh `C:\Users\gianf\` tree.

**FOUND — `cross_venue.py:233` prices both venues' fees at the Kalshi mid**,
not at the price each leg would execute at. Real, and **measured: the error is
at most 0.2c and always in the conservative direction** (mid overstates the
fee, because the fee peaks at 50c and execution is further out). So `M013`
("cross-venue MLB arbitrage structurally dead, 0 of 66") **stands, and is if
anything understated.** The quoted "6.75c two-venue fee" is a mid-price fee.

**LATENT, ZERO IMPACT — `analyse_families.py:200` does not clamp the price**
into 1–99 the way every other caller does. Confirmed the failure mode:
`cost_bar_cents(100, ...)` returns **entry fee 0.00c**, so an extreme-priced
family would be scored with no fee at all. **Then I checked the actual
artifact: 0 of 2,205 rows in `family_scorecard.json` have a median price that
rounds to 0 or 100.** Worth a one-line clamp; it changed nothing.

**NOTED — non-simultaneous quotes inside both arbitrage nulls.** `cross_venue.py`
fetches all Kalshi books, then crawls Polymarket; `threeway_sum.py` fetches
three legs as three paced calls. Same family as the skew that faked 1,292
arbitrages — **but staleness manufactures fake crossings, and both studies
found none.** `M012` (0 of 93) and `M013` are strengthened by this, not
weakened. The three "99c gross ask-sum" events cited under `M012` may be
timing artifacts; they were killed by fees regardless.

**CLEAN, and worth recording:** fee arithmetic all routes through
`common/costbar.py` → `kalshi_fees.py`, nothing in the allowlist and nothing
needs to be · the YES ask is derived as `100 − best NO bid` (GUARDS #7) ·
the three kill thresholds match `DECISIONS.md` D8 and were recorded while the
tape was still downloading, i.e. genuinely pre-registered · ladder rows are
deduped to events (GUARDS #8) · settlement read from the venue, never inferred
· no auth path and no POST anywhere in the folder.

### `kalshi-inplay-bot` + `kalshi-tennis` — read in full (52 files). Verified by me.

**⚠ THE ONE THAT MATTERS: the "dormant" tennis bot folder contains LIVE-MONEY
code.** `livedesk/src/demo_exec.py:113` builds its client as
`KalshiClient(demo=False, kill_switch=livedesk's own switch)` — importing
`kalshi-inplay-bot/kalshi_client.py`. **Verified.** So the folder that looks
switched-off since 3 August is the folder his real desk places orders through.
Anyone editing it is editing live-money code.

**Correction to the reader on this point.** It reported "no canary test here."
Partly wrong, and I checked: `livedesk/tests/test_one_switch_per_bot.py`
imports `KalshiClient` directly and asserts the switch behaviour **in both
directions**, and `_order()` itself validates side, price range and count
before posting. What is genuinely untested is the **payload shape** (the
price-as-dollar-string, count-as-string encoding at `kalshi_client.py:373-381`)
— livedesk's tests mock above that layer. Narrower exposure than reported,
still a real gap.

**FOUND — `scanner.py:136-138` places a take-profit SELL without waiting for
the BUY to fill.** Verified. `gui.py:759` was fixed to `await_fill` first, with
its own comment saying a sell on contracts you do not own "opens a SHORT";
`scanner.py` never got that fix. **Blocked in production by the kill switch,
but demo bypasses the switch by design** — so it can fire today on fake money,
and would be live the day the switch comes off.

**FOUND — the leaked-anchor price file is still wired into runnable code.**
`stage4_model.py:307` and `stage5_selective.py:124` still read
`kalshi_prematch_prices.parquet`, whose own sibling docstring calls it
"really the settled price... that is leakage". **No live conclusion rests on
it** — T007/T008/T010 were already retracted and T012 re-established the
direction on the clean anchor — but re-running those two scripts today
reproduces the leaked benchmark with no warning on screen.

**Also:** two `C:\Users\gianf\` paths in `kalshi-tennis/audit/` code that
HANDOFF tells a reader to run · `diag_gaps.py` is broken against its own API
and raises on first call · `gui.py:500` comment says the daily-loss breaker is
off, and it is on (safe direction, wrong docs) · `score_test.py`'s 2.9c maker
cost bar contradicts the folder's own finding that the maker fee is **zero** on
ITF/Challenger, ~91% of the tennis book, so a real maker edge under 2.9c there
would be declared untradeable by a bar that does not apply.

**CLEAN:** all eleven fee call sites import the shared module · every outcome
read from the venue, none inferred · backtest causality solid (entry on the
NEXT candle, exits scan only later, a look-ahead canary in the test file) ·
kill switch fail-closed at order time and git-tracked, no in-folder bypass.

### `bot-hunt` — read in full (~60 files). Verified by me.

**FOUND — esports ticker times parsed as UTC in one file and as Eastern in
four others.** `diagnose_cross.py:45` uses `tzinfo=timezone.utc`;
`crossvenue_arb.py:73`, `devig_where.py:49`, `devig_power.py:42` and
`paired_sampler.py:159` all use `America/New_York`. **Verified — the folder
contradicts itself.** The repo's own `BH012` established ET for MLB tickers,
exact against Pinnacle on 22 of 22. If ET is right for esports too, then
`h10_passive.py`'s `if ts >= et: skip` **discarded the last four genuinely
pre-match hours of every match** — the busiest window — and H10, the only
positive-direction result in the folder, describes a quieter regime than it
claims. No post-match leak is possible, so it is safe in direction, not in
description. **One venue cross-check settles it** and that is the fix I have
asked for, exactly the way BH012 did it.

**FOUND — declared latency constants that nothing uses.** `engine.py:55-56`
defines `LATENCY_MS_MIN/MAX` and `TAKER_EXTRA_LATENCY_MS`, the module header
lists latency among the "fill realism rules adopted", and **nothing references
them.** Every taker trade fills at the exact quote it decided on. The cited
source's own headline is that without latency most strategies look profitable.

**FOUND — day-one "qualifies" flags are fee-only.** `props_n3.py:258`,
`totals_n3.py:206`, `totals_family_n3.py:192`, `retail_n3.py:380` set
`bar = fee_rate_cents(ask)` with no spread or slippage term, while
`engine.cost_bar_cents` is half-spread + slippage + fee. Those runs mostly
conclude nothing qualifies, which the permissive bar makes *more* conservative
— but anything that ever survives one of those gates has cleared roughly half
the real cost of acting.

**RAISED ONCE, HIS CALL — a third-party API key is committed to this PUBLIC
repo.** `probe_pinnacle.py:40` carries `X-API-Key: CmX2...`. It is Pinnacle's
own public web-client guest key, shipped in their JavaScript to every browser,
and the file says so — **it is not his credential and leaks nothing of his.**
But `CLAUDE.md` §7 says never commit credentials, and this is credential-shaped.
Recorded here once; not raising it again.

**CLEAN:** every Kalshi fee call imports the shared module (the `0.07` literals
are docstring prose) · no analysis path marks at the mid, and the leak switch
`mark_to_mid` exists only so `validate_engine.py` can **fail** if the leak does
not inflate the number · settlement read from the venue everywhere · the
paired sampler fires both venues concurrently, stores the measured gap and
re-runs the skew placebo in every report · no POST, no order code, no
credentials of his anywhere in the folder.

### `bot-forensics` — read in full (22 files). **Two cited numbers move.**

Neither reverses a verdict. Both are quoted in documents he reads, and one is
quoted in `CLAUDE.md` itself, so both get corrected at source.

**⚠ `−0.77c net at ask` is really about `−0.37c`.** `t7_sweep.py:72` charges
`fee_order_cents(price, 1)` — the per-ORDER round-up applied to orders of ONE
contract. **`common/kalshi_fees.py` documents the right function for exactly
this** (`fee_rate_cents`: "for expectancy arithmetic, where the per-order
round-up is an artefact of order size rather than an economic cost") — I read
it to confirm. At ~90c entries the ceiling turns a 0.6c true fee into 1-2c.
The reader reproduced the holdout cell exactly (−0.770c, n=261) and
recomputed unrounded: **−0.374c.** So **+0.40c per contract of the cited
number is the rounding assumption, not economics.** Still negative, and the
6.06c spread is still the real killer, so **B024 stands** — but the quoted
magnitude is roughly double the fee-fair one.

**⚠ `97.4% of the price move had already happened` needs re-wording.**
`t2d_martingale.py:163` sets the "before" window as `mid − mid_p3`, which
**includes the very poll on which the score change first appears.** Median
tick spacing is 60s, so price and score share a row. Decomposed on the tapes:
of the +4.78c total repricing, **+2.83c (59%) fell strictly before the
score's poll, +1.80c (38%) in the same-poll interval, +0.15c (3%) after.**
So: **the operational claim is right at ~97** — the bot could not have acted,
because it only acts on its own ticks. **The causal claim "the feed was
minutes stale" is supported at ~59%, not 97.4.** Either number kills the
latency edge, which is why the verdict is untouched.

**Also found:** the desktop and laptop tapes interleave and their score caches
were out of sync — **243 of 4,995 score-change ticks are regressions** (a
games counter going DOWN), impossible in tennis, so up to ~10% of the n=4,398
lag sample is interleaving noise (symmetric — attenuates, does not
manufacture) · `engine.py:265` fills a stop at the stop price even when the
bid gapped straight through it, which makes stops look **better** than
reality, so **−9.36c is a ceiling and the real damage was worse** · the
"−2.29 → −9.36" sentence attributes to the stop what is partly the target
cap, though a clean stop-only isolation elsewhere agrees (+0.62c → −3.77c).

**CLEAN, and unusually good:** settlement cross-checked against an independent
fills-only reconstruction · the sell-as-buy-NO ambiguity was **tested, not
assumed** · everything parsed `utc=True`, no clock mixing anywhere · unit of
observation match-level then collapsed to 74 bursts, with the **wider** CI
chosen for the verdict · the bot/manual classifier is outcome-blind and its
own first version's failure is documented.

### `signal-github` + `social-signal` + `extractor-upgrade` — read (74 files).

The scoring arithmetic is sound — **the Wilson interval was checked against
the standard formula and is correct**, and no fee arithmetic exists in any of
the three. The defects cluster in one family: **a refusal recorded as a fact.**

**⚠ A GUARD THAT CANNOT FAIL.** `signal-github/src/kalshi_fees_census.py:110`
reads `check("makers and takers charged the same", False, False)` — **a
constant compared to a constant. It prints `[OK]` whatever the census found.**
Verified by reading it. The other three checks beside it are real. This sits
inside the script whose stated job is re-verifying the maker-fee correction
C1 — i.e. the fee question this repo already got wrong once. Exactly the
guard-rot GUARDS #9 exists for.

**Transient refusals cached permanently as death.**
`social-signal/src/verify_live.py:82` writes network errors and 429s into the
on-disk cache unconditionally, and the T4 report (the Reddit-COLLECT /
X-KILL / TikTok-KILL / Instagram-KILL verdicts) reads through that cache — so
**a one-off timeout becomes the permanent recorded status of a platform.**
The same folder's sibling `signal-github/src/gh.py:204` does it right and
says so in a comment: "404s are cached; transient failures are not."
`extractor-upgrade/src/verify_tech.py:133` has the same shape, and
`unify_currency.py:101` counts `COLD` as dead while its own docstring twelve
lines below says COLD is "reported as its own state, not as death" — so a
repo merely quiet for 13 months is reported as shut down.

**Absence claims built on silently incomplete inputs.** `hn.py:134` returns
`None` for a 429, a network error and a genuinely missing story alike —
the fake-zero pattern GUARDS #25 targets — and the auditor built to catch it
(`audit_fake_zero.py`) only scans `social-signal/src`, so this copy escapes ·
`gh.archive()` silently omits files over 512KB with no flag, and
`join_corpora.scan_corpus` never checks the truncation flags it does set, so
"appears in 0 of N source archives" can mean "we never stored the file" ·
comment threads truncate at 100 with no marker, while post sweeps correctly
print "CAPPED, not exhausted".

**A promised check that was never run.** `social-signal/src/db.py:150`
declares `n_check_verdict` and **nothing anywhere writes it** (verified: one
hit in the folder). The docstring says the n-check "exists to test whether a
stated sample can clear its own break-even" — so loaded Reddit win-rate
claims sit with a permanently empty verdict.

**CLEAN:** zero laptop paths · no hardcoded credentials, tokens from env,
`.env` gitignored with only an empty `.env.example` tracked, and token values
never printed · **`extractor-upgrade`'s read-only claim about sibling
databases is TRUE** — verified `mode=ro&uri=True` on all four.

### CLEAN — `mlb` analysis scripts and all of `reopen`

`rfi_market_calibration.py` is exemplary: two of its own past bugs documented
inline (in-inning quotes leaking into "pre-game", and an ask<100 filter that
had selected on the outcome). `reopen` writes only inside `reopen/`, network
is read-only public endpoints with an honest user-agent, and its screen
explicitly sorts-not-decides. No fee arithmetic, no laptop paths, no
credentials in either.
