# THE ASSUMPTION AUDIT — running record

**Started 2026-09-02 at his request, after his diagnosis: "the bot is assuming
too much."** Read-only against everything except this file and inline
corrections filed by house rules. Each finding names what was checked, what was
found, and where the fix was routed. **A clean check is recorded as loudly as a
catch, so the next reader knows what was already looked at.**

---

## CATCH 1 — my own calibration table used 22,974 rows the source study had rejected

`coordinator/studies/calib.py` filtered on `pre_bid IS NOT NULL` and never read
the `ok` flag that `set1_overshoot`'s `p6_state.py` stores on every row. 67% of
the sample was `pre-match book empty` — quote present, spread wider than
`PRE_SPREAD_MAX = 10`, mid meaningless. **Corrected inline in
`RESEARCH_PROGRAM.md` (commit `6b44936`), wrong table left visible.** Clean-row
story: gap smaller and patchy, two mid bands negative, one band (80–85c) at
**+0.8%** at the ask — not a finding. Practical conclusion unchanged. Tennis's
maker test unaffected (its bands were 96%+ clean rows).

**The rule this yields: an analysis that reads another study's table MUST honour
that study's own `ok`/`why` flags, and say so.**

## CLEAN — provenance of stored data against Kalshi's live API (2026-09-02)

| what | sample | result |
|---|---|---|
| tennis results, `maker.db` | 6 newest settled | **6/6 match** |
| baseball results, `kalshi_truth.db` | 8 newest settled | **8/8 match** |
| baseball minute candles, bid & ask | 10 candles | **10/10 match to the cent** |
| baseball earliest-price timing | all 1,706 | **all strictly before first pitch** |

**Limit stated:** spot checks on recent markets. Kalshi's ~69-day window means
older rows can never be externally verified again — the recorded copy is the
only copy, which is why the recorders exist. A standing re-run of this check
belongs in the recorder health pass, on markets young enough to verify.

## CORRECTION TO MY OWN CLAIM — "89 rules, a handful enforced" was wrong

`GUARDS.md` has **27 numbered guards** (my 89 counted every heading). Scorecard:

- **15 TESTED** — a test file references and asserts them (#1–7, 9, 13, 14, 18,
  20, 21, 22, 23)
- **9 referenced in code but not asserted by any test** (#8, 10, 11, 12, 15, 19,
  24, 25, 27)
- **3 prose only** (#16 dedup-headline, #17 argmax null, #26 rows-vs-people)

The enforcement picture is materially better than I reported. The gap that
remains: #10 (pre-registration before numbers) and #17 (argmax null) are the two
most load-bearing unenforced ones — both guard against exactly the best-of-N
failures this repo keeps having.

## CLEAN — the fee guard is exemplary, and my 12 "suspects" were already handled

`test_no_fee_reimplementation.py` scans the repo for fee-shaped arithmetic and
maintains a reasoned allowlist. All 12 files my sweep flagged are either
allowlisted with a written reason (quoted prose, refutation subjects, audits of
other people's claims) or non-matching. **Test passes.**

## CATCH 2 — two settled claims about Polymarket's fee disagree, and the sampler uses the weaker one

`C004` (SETTLED, 4,310 on-chain fills, April, reproduced on 5,362): the real
fee was `0.10 × min(p,1−p)` and the documented `0.07·p(1−p)` matched **0.0% of
fills**. `BH025` (three days old, docs page only, zero fills): sports are
`0.05·p(1−p)` taker. **Both can be right — different era, maybe different
products — but this repo has already proven once that Polymarket's docs did not
match what it charged.** Routed to `devig` (mailbox 026): verify against real
on-chain sports fills before the sampler's "executable after fees" column is
believed; until then the fee is labelled docs-only.

---

## STILL TO DO

1. **Assumed-numbers sweep beyond fees** — spreads, slippage constants,
   probabilities, sample-size claims hardcoded in active `src/` folders.
2. **The two unenforced load-bearing guards** (#10, #17) — scope what a test
   for each would look like; do not build without a decision.
3. **Recorder-side provenance** — the same spot-check discipline for
   `record.db`'s Polymarket and Pinnacle legs, which tonight's pass did not
   touch.
4. **Folder-by-folder pass** over the 13 test-less folders, dormant ones last.

---

## PASS 3 — overnight run, 2026-09-01

## FALSE ALARM CAUGHT IN MYSELF — the "recorder dead for two days" scare was my own clock

The newest tape rows read `2026-09-01T04:48Z` and I read that against sync
dates I had been writing as September 2nd. **Local time was 00:48 on September
1st — the recorder had written 14 minutes earlier.** Every runner is alive:
both cross-venue recorders, the factory's wide recorders, both paper tests,
**and the paired sampler and props capture are already built and registered in
`runners/runners.json`** — devig had finished and wired them without being
chased. My recent sync-marker dates had drifted a day ahead; same class of
error as the 6.5-minute clock skew that faked 1,292 arbitrages. Corrected.

## ASSUMED CONSTANT — `SLIPPAGE_C = 1.0` gates every baseball bot entry, never measured

`mlb-paper/src/mentalities.py:79`. A flat assumed cent subtracted from every
edge before a bot may enter, in the live decision path and the replay. It
shapes WHICH bets fire. **Measurable now from `livedesk`'s real fills** (asked
36c, filled 33c — the desk records both sides). Routed to `mlb` as a note.

## PROVENANCE, other two recorder legs — instrument works, hour was wrong

Polymarket: the public book endpoint 403s a bare client and answers one with a
browser user-agent; at 1am every book was settled/empty (1/99 placeholders), so
the value comparison was **inconclusive, not failed** — `test_poly_sides.py`
already guards orientation. Pinnacle: guest API live, 1,149 markets, zero
overlap with recorded rows because those games settled. **Both re-runs queued
for game hours.**

## EVIDENCE-FILE SCAN — 651 references behind all seven ledgers

**627 resolve on disk. 1 is data not on this machine. 23 are truly missing —
and they cluster:** ~19 are pre-migration scripts behind still-cited SETTLED
claims, including `src/p5_segment.py` (S005, "0 of 25 buckets clear"),
`src/phase4b_shrinkage.py` (W015, the shrinkage result), `src/exit_study.py`
and `src/hold_settle.py` (C022/C023 era), plus chat-export JSONs that
`LAPTOP_COPY.md` already documents as laptop-only. **The claims are not
suspect — their generating scripts live on the laptop and never made the
machine switch.** Fix is a two-minute copy next time the laptop is open;
exact list kept here.

## SCOPED, NOT BUILT — tests for the two load-bearing prose guards

**#10 (pre-registration before numbers):** mechanically testable — for every
`reports/`-style results file, assert a `PREREGISTRATION_*.md` exists in the
folder whose FIRST git commit predates the results file's first commit.
Grandfather existing files by date. ~60 lines plus the grandfather list.
**#17 (the argmax null):** not mechanically testable without false positives;
the honest home is a required line in `coordinator/REFLECT.md`'s Critic list —
"where a best-of-N appears, the N and a shuffle control must appear beside
it." Both are decisions for him, not built.

## STILL OPEN FROM PASS 2
Recorder-leg value checks in game hours (mine) · assumed-numbers sweep beyond
fees and slippage (partial) · folder-by-folder over the 13 test-less folders
(the cited-claims part is covered by the evidence scan; code-reading pass
remains).
