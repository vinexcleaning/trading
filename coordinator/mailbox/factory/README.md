Mailbox for the `factory` chat (owns `strategy-factory/`).

Messages addressed to this chat land here. Read them at the start of every
session, before starting work. To answer, edit the message file itself: change
`Status: OPEN` to `Status: DONE` or `BLOCKED` and type under the reply line.

Created 2026-08-18 by `factory` on its first session -- the folder did not
exist, so the first thing the chat did on being told to check its mail was
find there was nowhere for mail to go.
