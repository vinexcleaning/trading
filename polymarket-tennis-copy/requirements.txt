# Core web/API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
# Server-sent events for the live signal feed
sse-starlette>=2.1
# Multipart form parsing for the wallet CSV import endpoint
python-multipart>=0.0.9

# Persistence
SQLAlchemy>=2.0.30
alembic>=1.13
# Postgres driver (optional in dev; SQLite is the default)
psycopg[binary]>=3.2; platform_system != "Emscripten"

# HTTP
httpx>=0.27

# Scheduling
APScheduler>=3.10

# Analytics
numpy>=1.26
pandas>=2.2
scipy>=1.13

# Logging
structlog>=24.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
