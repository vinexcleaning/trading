[pytest]
testpaths = backend/tests
pythonpath = backend
addopts = -q --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    live: hits real network endpoints (deselected by default via -m "not live")
